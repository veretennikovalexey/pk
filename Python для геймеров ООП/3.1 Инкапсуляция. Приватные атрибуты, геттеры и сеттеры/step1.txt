hero = Character("Шэдоухарт", "Высший полуэльф", 10, 13)
print(hero.health)
hero.health = 100

# public

class Character:
    def __init__(self, name, health):
        self.name = name
        self.health = health

hero = Character("Шэдоухарт", 10)
print(hero.health)
hero.health = 25
print(hero.health)

hero.health = -999

# Пример 2. __Приватные атрибуты

class Character:
    def __init__(self, name, health):
        self.__name = name
        self.__health = health

hero = Character("Гейл", 8)
print(hero.__health)

print(hero._Character__health)

# Пример 3. _Полуприватные атрибуты

